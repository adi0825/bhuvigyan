server:
  port: 8085
spring:
  application:
    name: land-api-service
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/
app:
  dev-mode: ${DEV_MODE:true}
kgis:
  api:
    base-url: https://kgis.ksrsac.in:9000
